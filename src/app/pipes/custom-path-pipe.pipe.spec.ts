import { CustomPathPipePipe } from './custom-path-pipe.pipe';

describe('CustomPathPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPathPipePipe();
    expect(pipe).toBeTruthy();
  });
});
