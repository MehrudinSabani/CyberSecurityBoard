import { FilterByPathIdPipe } from './filter-by-path-id.pipe';

describe('FilterByPathIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByPathIdPipe();
    expect(pipe).toBeTruthy();
  });
});
