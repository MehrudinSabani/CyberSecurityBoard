import { AppFullScreenDirective } from './app-full-screen.directive';

describe('AppFullScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFullScreenDirective();
    expect(directive).toBeTruthy();
  });
});
